/** @type {import('tailwindcss').Config} */
module.exports = {
  // Scans the single-page app (markup + class strings inside the inline <script>)
  content: ['./index.html'],
  theme: { extend: {} },
  plugins: [],
};
